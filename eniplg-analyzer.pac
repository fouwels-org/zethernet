# Generated by binpac_quickstart

refine flow ENIPLG_Flow += {
	function proc_eniplg_message(msg: ENIPLG_PDU): bool
		%{
		BifEvent::generate_eniplg_event(connection()->bro_analyzer(), connection()->bro_analyzer()->Conn());
		return true;
		%}
};

refine typeattr ENIPLG_PDU += &let {
	proc: bool = $context.flow.proc_eniplg_message(this);
};